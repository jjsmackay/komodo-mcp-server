# Security hardening
audit-level=high
strict-ssl=true
ignore-scripts=true

# Reproducibility
engine-strict=true       # Strictly enforce the Node >=20 engines constraint
fund=false               # Reduce CI output noise

# Registry pinning (defense against typosquatting via alternative registries)
registry=https://registry.npmjs.org/